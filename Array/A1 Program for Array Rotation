#include <iostream>
#include <vector>

using namespace std;

/*
int main()
{
	int n;
	vector<int>vec;
	while( cin>>n )
	{
		if(n == '\n')
		break;
		vec.push_back(n);
	}
	for(int i=0;i<vec.size();i++)
	{
		cout<<vec[i]<<" ";
	}
	cout<<endl<<n;
}
*/

/*
int main()
{
	string s;
	
	getline(cin,s);
	
	int n = s.length();
	for(int i=0;i<n;i++)
	{
		cout<<s[i]<< " ";
	}
	
	return 0;
}
*/


int main()
{
	string s;
	
	getline(cin,s);
	
	cout<<s;
	
	return 0;
}
